query RunInput {
  discount: discountNode {
    halfAmount: metafield(namespace: "$app:discount", key: "half-amount") { value }
    halfPercentage: metafield(namespace: "$app:discount", key: "half-amount") { value }
  }
  cart {
    plan: attribute(key: "Payment Plan") { value }
  }
}
